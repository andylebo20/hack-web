import React from "react";

export const PropertyScreen = () => {
  return (
    <div>
      This is the screen when a renter clicks on a property and it shows more
      info and lets them book if they want
    </div>
  );
};
